// Package scheduler is the single 60-second ticker: it keeps weather and Tado
// fresh and sends the daily digest and alerts. It does not compute due dates
// or format messages — those stay in care and notify.
package scheduler
