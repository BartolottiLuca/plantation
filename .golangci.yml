run:
  timeout: 3m
  go: "1.23"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - revive
    - ineffassign

linters-settings:
  revive:
    rules:
      - name: exported
        disabled: true
      - name: package-comments
        disabled: false
